node_modules/
.expo/
dist/
web-build/

# Expo Continuous Native Generation — native projects are regenerated
# via `npx expo prebuild`, not committed. Add custom native code through
# config plugins instead.
ios/
android/

npm-debug.*
yarn-debug.*
yarn-error.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env
.env.*
.DS_Store
